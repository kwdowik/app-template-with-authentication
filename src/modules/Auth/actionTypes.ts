const NAME: string = 'Auth';

export const LOGIN: string = `${NAME}/LOGIN`;
export const LOGIN_SUCCEED: string = `${NAME}/LOGIN_SUCCEED`;
export const LOGIN_FAILED: string = `${NAME}/LOGIN_FAILED`;

export const LOGOUT: string = `${NAME}/LOGOUT`;

export const SET_TOKEN: string = `${NAME}/SET_TOKEN`;
export const REMOVE_TOKEN: string = `${NAME}/REMOVE_TOKEN`;
